<div class="container">
  <h1>votre profil: <br>     <strong><%= current_user.first_name%> <%= current_user.last_name%></strong></h1>
  <hr>
  <h2>Adresse: prevoir carte</h2>
  <p><%= current_user.address%></p>
  <hr>
  <h2>Description:</h2>
  <p><%= current_user.description%></p>
  <hr>
  <div>
    <%= link_to "Proposer une chévre", locations_url, class: "btn btn-primary" %>
    <%# verifier l'url ci dessus pour le renvois vers location %>
    <%= link_to "Mes p'tites chévres", goats_path, class: "btn btn-primary" %>
  </div>
</div>


